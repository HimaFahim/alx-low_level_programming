#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Write a program that prints all single digit numbers of base 10
 *
 * Return: 0 (success)
 *
*/

int main(void)
{
	int i = 0;
		for (; i <= 10; i++)
			printf("%d", i);
	return (0);
}
